'use client'

// Re-export Radix-backed shadcn button with custom styling
// (single source of truth — atoms/button re-exports this)
export { Button, buttonVariants } from '../../atoms/button'
export type { ButtonProps } from '../../atoms/button'
